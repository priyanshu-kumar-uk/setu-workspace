import React from 'react'import { authGetme } from '../features/auth/hooks/api.hooks.js'import { Navigate, Outlet } from 'react-router-dom'const ProtectedRoute = () => {  const { data } = authGetme();  if (!data) return null;   return <Outlet />;}export default ProtectedRoute